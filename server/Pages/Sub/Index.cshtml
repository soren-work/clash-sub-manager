@page "/sub/{id?}"
@model ClashSubManager.Pages.Sub.IndexModel
@{
    Layout = null;
    Response.ContentType = "text/yaml";
}

@* This page handles API endpoints and returns YAML or JSON responses *@
@* The actual response is handled in the PageModel code-behind *@
